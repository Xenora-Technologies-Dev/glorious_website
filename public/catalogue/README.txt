Drop catalogue product photography here using the product slug as the filename.

Examples:
  pasta.jpg
  olive-oil.jpg
  tomato-ketchup.jpg

When a file is present, set `catalogueImage` on that product in src/content/products.ts so the site uses it instead of atmospheric fallback photography.
